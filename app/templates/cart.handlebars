<h1>This is a cart</h1>

<div class='row'>
  <div class='col-lg-2'>
   {{# each items }}
  <li>
    <img {{ bind-attr src=image }} class='thumbnail col-lg-12' />

    <button {{ action 'deleteItem' this }}>Delete</button>
  </li>
  {{/ each}}
  </div>
</div>